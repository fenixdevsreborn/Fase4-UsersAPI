version: '3.9'

services:

  users-api:
    build: .
    ports:
      - "8080:8080"

    environment:
      ASPNETCORE_ENVIRONMENT: Development
      Redis__Connection: redis:6379

    depends_on:
      - redis

  redis:
    image: redis:7
    ports:
      - "6379:6379"